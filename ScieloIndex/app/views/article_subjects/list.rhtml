<h1>Listing article_subjects</h1>

<table>
  <tr>
  <% for column in ArticleSubject.content_columns %>
    <th><%= column.human_name %></th>
  <% end %>
  </tr>

<% for article_subject in @collection %>
  <tr>
  <% for column in ArticleSubject.content_columns %>
    <td><%=h article_subject.send(column.name) %></td>
  <% end %>
    <td><%= link_to 'Show', :action => 'show', :id => article_subject %></td>
    <td><%= link_to 'Edit', :action => 'edit', :id => article_subject %></td>
    <td><%= link_to 'Destroy', { :action => 'destroy', :id => article_subject }, :confirm => 'Are you sure?', :method => :post %></td>
  </tr>
<% end %>
</table>
<div id="buttonsNavigator">
<%= link_to 'Previous page', { :page => @pages.current.previous }, :class => 'cssbutton' if @pages.current.previous %>
<%= link_to 'Next page', { :page => @pages.current.next }, :class => 'cssbutton' if @pages.current.next %>
</div>
<br />
<div id="buttonsNavigator">
<%= link_to 'New article_subject', {:action => 'new'}, :class => 'cssbutton' %>
</div>